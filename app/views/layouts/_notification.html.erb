<div class="curtain">
  <div class="row pad-top-large">
    <div class="white-box award-box">
      <i class="close">X</i>
      <h1 class="pad-bottom">Congratulations!</h1>
      <p>You have achieved the following awards, keep up the good work!</p>
      <% awards.each do |a| %>
        <div class="award margin-top">
          <%= image_tag a.badge.icon %>
          <h2><%= a.badge.name %> <%= ": #{current_user.title.name}" if a.badge.id == 1 %><h2>
        </div>
        <% mark_notified(a) %>
      <% end %>
      <%= link_to "View Stats", creative_user_path(current_user), class: "button margin-top-large" %> 
    </div>
  </div>
</div>